 if @vote.valid? 
 @choice.poll.id.to_s 
 (render :partial => 'polls/poll_ui', :locals => {:poll => @choice.poll}).gsub('"', '\\"').gsub("\n", "").html_safe 
 else 
 @vote.errors.full_messages.join(', ') 
 end 
